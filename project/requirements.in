

# HTTP client for llama-server calls
httpx>=0.25.0

# Tokenizer compatible with LLaMA and similar models
tiktoken>=0.4.0

# Word document parsing
python-docx>=0.8.11

# Progress bars for feeding/updating chunks
tqdm>=4.66.1

# Vector embeddings for RAG
sentence-transformers>=2.2.2

# Vector similarity search
faiss-gpu #faiss-cpu>=1.7.4  # use faiss-gpu if you have GPU and want faster similarity search

# Optional: numpy for embeddings and FAISS
numpy<2
